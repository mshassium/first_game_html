*** a/game/index.html
--- b/game/index.html
@@
     .menu {
       position: fixed;
       inset: 0;
       z-index: 280;
       display: flex;
       align-items: center;
       justify-content: center;
       background: #0c1014;
     }
 
     .main-menu {
       background: #10182a;
       border-radius: 24px;
       box-shadow: 0 10px 24px rgba(0,0,0,.5);
-      padding: 40px 32px 32px 32px;
+      /* чуть компактнее сверху, т.к. карточные буквы заняли место заголовка */
+      padding: 28px 32px 28px 32px;
     }
 
     .menu .cardmenu {
       background: linear-gradient(180deg, #0f1728, #0b1220);
       border: 1px solid #2d3754;
       border-radius: 20px;
       padding: 22px;
       width: min(520px, 92vw);
       box-shadow: 0 28px 56px rgba(0, 0, 0, .55);
       display: flex;
       flex-direction: column;
       align-items: center;
       overflow: hidden;
     }
 
-    .main-menu {
-      padding: clamp(20px, 5vh, 40px) clamp(16px, 4vw, 32px);
-      gap: clamp(16px, 3vh, 32px)
-    }
+    .main-menu {
+      padding: clamp(20px, 4.2vh, 32px) clamp(16px, 4vw, 32px);
+      gap: clamp(16px, 2.6vh, 28px)
+    }
 
-    .main-title {
-      font-size: clamp(36px, 8vh, 72px);
-      font-weight: 900;
-      color: #9cc8ff;
-      text-shadow: 0 0 30px #9cc8ff66, 0 2px 4px rgba(0,0,0,0.8);
-      margin-bottom: clamp(8px, 2vh, 24px);
-      letter-spacing: clamp(1px, 0.6vw, 4px)
-    }
+    /* заголовок удалён */
 
     .menu .muted {
       color: #acb6cf;
       font-size: 13px;
       margin-bottom: 12px
     }
 
     .menu .items {
       display: flex;
       flex-direction: column;
       align-items: center;
-      gap: clamp(12px, 2.5vh, 16px);
+      gap: clamp(12px, 2.2vh, 16px);
       width: 100%;
-      margin-bottom: clamp(16px, 3vh, 32px)
+      margin-bottom: clamp(10px, 2.4vh, 24px)
     }
 
     .menu .btn.big {
       font-size: clamp(14px, 2.4vh, 18px);
       font-weight: 700;
       padding: clamp(10px, 2.6vh, 18px) clamp(16px, 4.2vh, 32px);
       width: 100%;
       max-width: 320px;
       border-radius: 16px
     }
@@
-    .menu-cards {
-      display: flex;
-      gap: clamp(10px, 2vh, 16px);
-      justify-content: center;
-      align-items: center;
-      margin-top: clamp(10px, 2vh, 16px)
-    }
+    .menu-cards {
+      display: flex;
+      gap: clamp(12px, 2.2vh, 18px);
+      justify-content: center;
+      align-items: center;
+      /* переносим блок на место заголовка и даём «геройскую» высоту */
+      margin-top: 0;
+      margin-bottom: clamp(16px, 3.2vh, 28px);
+    }
 
-    .menu-card {
-      width: clamp(40px, 9vh, 64px);
-      height: clamp(40px, 9vh, 64px);
+    .menu-card {
+      /* крупнее, чтобы ощущаться как заголовок */
+      width: clamp(52px, 9.5vh, 84px);
+      height: clamp(52px, 9.5vh, 84px);
       border-radius: 10px;
       display: flex;
       align-items: center;
       justify-content: center;
-      font-size: clamp(20px, 4vh, 32px);
+      font-size: clamp(26px, 4.4vh, 40px);
       font-weight: 900;
       position: relative;
       box-shadow: 0 0 0 3px transparent, 0 4px 12px rgba(0,0,0,0.4);
       transition: all 0.3s ease;
       cursor: pointer;
     }
@@
     @media (hover: hover) {
       .menu-card:hover {
         transform: translateY(-4px) scale(1.05);
         box-shadow: 0 0 0 3px currentColor, 0 8px 24px rgba(0,0,0,0.5);
       }
     }
@@
-    /* Активное состояние по тапу (мобайлы) */
+    /* Активное состояние по тапу (мобайлы) */
     .menu-card.menu-card-active {
       transform: translateY(-4px) scale(1.05);
       box-shadow: 0 0 0 3px currentColor, 0 8px 24px rgba(0,0,0,0.5);
     }
@@
     @media (max-width: 900px) {
@@
-      .main-title {
-        font-size: 48px;
-        margin-bottom: 16px;
-        letter-spacing: 2px
-      }
-
       .main-menu {
-        padding: 24px 20px
+        padding: 18px 18px
       }
 
       .menu-cards {
-        gap: 12px;
-        margin-top: 12px
+        gap: 10px;
+        margin-top: 0;
+        margin-bottom: 14px;
       }
 
       .menu-card {
-        width: 48px;
-        height: 48px;
-        font-size: 24px
+        width: 56px;
+        height: 56px;
+        font-size: 26px
       }
 
       .menu .btn.big {
         font-size: 16px;
         padding: 14px 24px
       }
     }
 
     @media (max-width: 600px) {
@@
-      .main-title {
-        font-size: 13px
-      }
-
       .pill {
         padding: 3px 5px;
         font-size: 10px
       }
@@
-      .menu-cards {
-        gap: 12px;
-        margin-top: 12px
-      }
+      .menu-cards { gap: 8px; margin-top: 0; margin-bottom: 12px; }
 
-      .menu-card {
-        width: 48px;
-        height: 48px;
-        font-size: 24px
-      }
+      .menu-card { width: 48px; height: 48px; font-size: 22px; }
 
       .menu .btn.big {
         font-size: 16px;
         padding: 14px 24px
       }
     }
@@
   <div id="menu" class="menu" style="display:flex">
     <div class="cardmenu main-menu">
-      <div class="main-title">F!RST</div>
-      <div class="items">
-        <button class="btn big modern" id="menuNewGame">New Game</button>
-        <button class="btn big modern" id="menuRules">Rules</button>
-        <button class="btn big modern" id="menuLangBtn">🇬🇧 EN</button>
-      </div>
-      <div class="menu-cards">
+      <!-- карточное слово перемещено на место заголовка -->
+      <div class="menu-cards">
         <div class="menu-card card-f">F</div>
         <div class="menu-card card-i">I</div>
         <div class="menu-card card-r">R</div>
         <div class="menu-card card-s">S</div>
         <div class="menu-card card-t">T</div>
       </div>
+      <div class="items">
+        <button class="btn big modern" id="menuNewGame">New Game</button>
+        <button class="btn big modern" id="menuRules">Rules</button>
+        <button class="btn big modern" id="menuLangBtn">🇬🇧 EN</button>
+      </div>
     </div>
   </div>
